{
  "name": "billion-blocks-ds",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "npx nuxt build",
    "dev": "npx nuxt dev",
    "format": "prettier --write .",
    "generate": "npx nuxt generate",
    "preview": "npx nuxt preview"
  },
  "dependencies": {
    "nuxt": "^3.14.0",
    "vue": "^3.5.0"
  },
  "devDependencies": {
    "prettier": "^3.4.2",
    "typescript": "^5.6.0"
  }
}
