{
  "name": "billion-blocks-ds",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "npx nuxt build",
    "dev": "npx nuxt dev",
    "generate": "npx nuxt generate",
    "preview": "npx nuxt preview"
  },
  "dependencies": {
    "nuxt": "^3.14.0",
    "vue": "^3.5.0"
  },
  "devDependencies": {
    "typescript": "^5.6.0"
  }
}
